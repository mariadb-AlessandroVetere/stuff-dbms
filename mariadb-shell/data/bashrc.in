source ~$USER/.bashrc
export bush_name=\$(basename \$HOME)
export PS1=\"{\$bush_name} \${PS1}\"
export CDPATH=\".:~\"

reconf()
{
    source ~/.bashrc
}

source ~/env.sh

[ \"\$STY\" ] && trap 'debug_trap' DEBUG
